name: 'UCI_Student_Info'
version: '1.0.0'
config-version: 2

profile: 'UCI_Student_Info'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  UCI_Student_Info:
    example:
      +materialized: view
